<Window x:Class="Camera.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Camera" d:DataContext="{d:DesignInstance Type=local:ViewModelMainWindow}"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="60"></RowDefinition>
            <RowDefinition Height="390*"></RowDefinition>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0">

            <Grid.RowDefinitions>
                <RowDefinition Height="30"></RowDefinition>
                <RowDefinition Height="30"></RowDefinition>
            </Grid.RowDefinitions>

            <Grid Grid.Row="0">
                
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="150"></ColumnDefinition>
                    <ColumnDefinition Width="400"></ColumnDefinition>
                    <ColumnDefinition Width="250"></ColumnDefinition>
                </Grid.ColumnDefinitions>

                <Button Content="Search device" Margin="5" Grid.Column="0" 
                Command="{Binding SearchDeviceCommand}"/>

                <ComboBox x:Name="cbDeviceList" Margin="2" Grid.Column="1"
                      ItemsSource="{Binding NameCamers, Mode=OneWay}" SelectedItem="{Binding NameSelectDevice, Mode=OneWayToSource}" RenderTransformOrigin="0.5,0.5">
                </ComboBox>

            </Grid>

            <Grid Grid.Row="1">
                
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="70"></ColumnDefinition>
                    <ColumnDefinition Width="70"></ColumnDefinition>
                    <ColumnDefinition Width="60"></ColumnDefinition>
                    <ColumnDefinition Width="400"></ColumnDefinition>
                    <ColumnDefinition Width="250"></ColumnDefinition>
                </Grid.ColumnDefinitions>
                
                <Button x:Name="StartStopGrabButton" Margin="5" Grid.Column="0" 
                        Command="{Binding StartGrabCommand}">

                    <Button.Style>
                        <Style TargetType="Button">
                            <Setter Property="Content" Value="Start grab"/>
                            <Setter Property="IsEnabled" Value="False"/>
                            <Style.Triggers >

                                <DataTrigger Binding="{Binding SelectCamera.IsCreate}" Value="True">
                                    <Setter Property="IsEnabled" Value="True" />
                                </DataTrigger>

                                <DataTrigger Binding="{Binding SelectCamera.IsGrab}" Value="True ">
                                    <Setter Property="Content" Value="Stop grab" />
                                </DataTrigger>

                            </Style.Triggers>
                        </Style>
                    </Button.Style>
                </Button>

                <Button x:Name="OpenCloseButton" Margin="5" Grid.Column="1" 
                         Command="{Binding CreateDeviceCommand}">
                    <Button.Style>
                        <Style TargetType="Button">
                            <Setter Property="IsEnabled" Value="False"/>
                            <Setter Property="Content" Value="Create"/>
                            <Style.Triggers >

                                <DataTrigger Binding="{Binding AreAnyDevices}" Value="True">
                                    <Setter Property="IsEnabled" Value="True" />
                                </DataTrigger>

                                <DataTrigger Binding="{Binding SelectCamera.IsCreate}" Value="True">
                                    <Setter Property="Content" Value="Destroy" />
                                </DataTrigger>

                            </Style.Triggers>
                        </Style>
                    </Button.Style>
                </Button>



                <TextBlock Text="Message:" Margin="5" Grid.Column="2"/>

                <TextBlock Text="{Binding Feedback}" Margin="5" Grid.Column="3"/>

            </Grid>



           

        </Grid>

        <Grid Grid.Row="1" >

            <Grid.ColumnDefinitions >
                <ColumnDefinition Width="550*"/>
                <ColumnDefinition Width="250*"/>
            </Grid.ColumnDefinitions>

            <Grid Grid.Column="1" HorizontalAlignment="Right">

                <Grid.RowDefinitions >
                    <RowDefinition Height="20"></RowDefinition>
                    <RowDefinition Height="20"></RowDefinition>
                    <RowDefinition Height="20"></RowDefinition>
                    <RowDefinition Height="200"></RowDefinition>
                </Grid.RowDefinitions>


                <TextBlock Text="CreateCamers:" Grid.Row="0"/>

                <ComboBox x:Name="cbCreateDeviceList" Grid.Row="1"
                            ItemsSource="{Binding NameCreateCamera, Mode=OneWay}"/>

                <TextBlock Text="Parametrs:" Grid.Row="2"/>

                <Grid Grid.Row="3">

                    <Grid.RowDefinitions>
                        <RowDefinition Height="20"></RowDefinition>
                        <RowDefinition Height="20"></RowDefinition>
                        <RowDefinition Height="20"></RowDefinition>
                        <RowDefinition Height="30"></RowDefinition>
                        <RowDefinition Height="30"></RowDefinition>
                        <RowDefinition Height="30"></RowDefinition>
                        <RowDefinition Height="30"></RowDefinition>
                    </Grid.RowDefinitions>

                    <Grid.ColumnDefinitions >
                        <ColumnDefinition Width="125"/>
                        <ColumnDefinition Width="70"/>
                        <ColumnDefinition Width="55"/>
                    </Grid.ColumnDefinitions>

                    <TextBlock Text="Exposure Time:" Grid.Row="0" Grid.Column="0"/>

                    <TextBox Grid.Row="0" Grid.Column="1"
                                Text="{Binding SelectCamera.ExposureTime, Mode=TwoWay}">

                        <TextBox.Style>
                            <Style TargetType="TextBox">
                                <Setter Property="IsEnabled" Value="False"/>
                                <Style.Triggers >

                                    <DataTrigger Binding="{Binding SelectCamera.IsCreate}" Value="True">
                                        <Setter Property="IsEnabled" Value="True" />
                                    </DataTrigger>

                                </Style.Triggers>
                            </Style>
                        </TextBox.Style>
                    </TextBox>

                    <TextBlock Text="Gain:" Grid.Row="1" Grid.Column="0"/>

                    <TextBox Grid.Row="1" Grid.Column="1"
                                Text="{Binding SelectCamera.Gain, Mode=TwoWay}">
                        <TextBox.Style>
                            <Style TargetType="TextBox">
                                <Setter Property="IsEnabled" Value="False"/>
                                <Style.Triggers >

                                    <DataTrigger Binding="{Binding SelectCamera.IsCreate}" Value="True">
                                        <Setter Property="IsEnabled" Value="True" />
                                    </DataTrigger>

                                </Style.Triggers>
                            </Style>
                        </TextBox.Style>
                    </TextBox>

                    <TextBlock Text="Frame Rate:" Grid.Row="2" Grid.Column="0"/>

                    <TextBox Grid.Row="2" Grid.Column="1"
                                Text="{Binding SelectCamera.FrameRate, Mode=TwoWay}">

                        <TextBox.Style>
                            <Style TargetType="TextBox">
                                <Setter Property="IsEnabled" Value="False"/>
                                <Style.Triggers >

                                    <DataTrigger Binding="{Binding SelectCamera.IsCreate}" Value="True">
                                        <Setter Property="IsEnabled" Value="True" />
                                    </DataTrigger>

                                </Style.Triggers>
                            </Style>
                        </TextBox.Style>
                    </TextBox>

                    <Button Content ="Get Auto Exposure" Margin ="5" Grid.Row="3" Grid.Column="0"
                             Command="{Binding GetAutoExposureCommand}">

                        <Button.Style>
                            <Style TargetType="Button">
                                <Setter Property="IsEnabled" Value="False"/>
                                <Style.Triggers >

                                    <DataTrigger Binding="{Binding SelectCamera.IsCreate}" Value="True">
                                        <Setter Property="IsEnabled" Value="True" />
                                    </DataTrigger>

                                </Style.Triggers>
                            </Style>
                        </Button.Style>
                    </Button>

                    <Button Content ="Set Auto Exposure" Margin ="5" Grid.Row="4" Grid.Column="0"
                                Command="{Binding SetAutoExposureCommand}">

                        <Button.Style>
                            <Style TargetType="Button">
                                <Setter Property="IsEnabled" Value="False"/>
                                <Style.Triggers >

                                    <DataTrigger Binding="{Binding SelectCamera.IsCreate}" Value="True">
                                        <Setter Property="IsEnabled" Value="True" />
                                    </DataTrigger>

                                </Style.Triggers>
                            </Style>
                        </Button.Style>
                    </Button>

                    <TextBlock Text="MaxTime:" Margin ="5" Grid.Row="3" Grid.Column="1"/>

                    <TextBox Grid.Row="3" Grid.Column="2"
                             Text="{Binding SelectCamera.TimeUpper, Mode=TwoWay}" Grid.ColumnSpan="2">
                        <TextBox.Style>
                            <Style TargetType="TextBox">
                                <Setter Property="IsEnabled" Value="False"/>
                                <Style.Triggers >

                                    <DataTrigger Binding="{Binding SelectCamera.IsCreate}" Value="True">
                                        <Setter Property="IsEnabled" Value="True" />
                                    </DataTrigger>

                                </Style.Triggers>
                            </Style>
                        </TextBox.Style>
                    </TextBox>

                    <TextBlock Text="MinTime:" Margin ="5" Grid.Row="4" Grid.Column="1"/>
                    
                    <TextBox Grid.Row="4" Grid.Column="2"
                                Text="{Binding SelectCamera.TimeLower, Mode=TwoWay}" Grid.ColumnSpan="2">
                        <TextBox.Style>
                            <Style TargetType="TextBox">
                                <Setter Property="IsEnabled" Value="False"/>
                                <Style.Triggers >

                                    <DataTrigger Binding="{Binding SelectCamera.IsCreate}" Value="True">
                                        <Setter Property="IsEnabled" Value="True" />
                                    </DataTrigger>

                                </Style.Triggers>
                            </Style>
                        </TextBox.Style>
                    </TextBox>

                    <Button Content ="Get Param" Margin ="5"  Grid.Row="5" Grid.Column="0"
                            Command="{Binding GetParamCommand}">

                        <Button.Style>
                            <Style TargetType="Button">
                                <Setter Property="IsEnabled" Value="False"/>
                                <Style.Triggers >

                                    <DataTrigger Binding="{Binding SelectCamera.IsCreate}" Value="True">
                                        <Setter Property="IsEnabled" Value="True" />
                                    </DataTrigger>

                                </Style.Triggers>
                            </Style>
                        </Button.Style>
                    </Button>

                    <Button Content ="Set Param" Margin ="5" Grid.Row="6" Grid.Column="0"
                            Command="{Binding SetParamCommand}">

                        <Button.Style>
                            <Style TargetType="Button">
                                <Setter Property="IsEnabled" Value="False"/>
                                <Style.Triggers >

                                    <DataTrigger Binding="{Binding SelectCamera.IsCreate}" Value="True">
                                        <Setter Property="IsEnabled" Value="True" />
                                    </DataTrigger>

                                </Style.Triggers>
                            </Style>
                        </Button.Style>
                    </Button>
                </Grid>
            </Grid>

            <Image x:Name="CameraImage1" Grid.Column="0" Margin ="20"
                    Source="{Binding SelectCamera.InImg}" >
            </Image>
        </Grid>
    </Grid>
</Window>